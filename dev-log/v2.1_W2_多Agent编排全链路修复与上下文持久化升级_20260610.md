# v2.1 W2 — 多Agent编排全链路修复与上下文持久化升级

> **日期**: 2026-06-10 | **状态**: ✅ 完成

---

## 一、修复总览

本阶段完成了 5 个批次的修复，覆盖 **路由、上下文、持久化、版本控制、流式处理、容量限制** 6 个维度，共改动 **18 个文件**。

---

## 二、各批次详情

### 批次 1: 编排器 2.0 基础修复

| 修复 | 文件 | 内容 |
|------|------|------|
| agent_doc 404 | `server/src/routes/agents.ts` | 在 BUILT_IN_AGENTS 中添加 agent_doc |
| 编排器 2.0 接入 | `appStore.ts` | `runOrchestrated` 改为调用后端 `apiPlanTasks` + `selectAgent` |
| CSP 禁用 | `index.html` + `ArtifactPanel.tsx` | 移除主页和 iframe 的 CSP meta 标签 |
| Monaco 修复 | `ArtifactPanel.tsx` | DiffEditor 和 Editor 添加 `key` prop 防止 model disposal |
| 模型切换 | `server/src/services/llmGateway.ts` | `deepseek-chat` → `deepseek-v4-flash` |
| 后端日志 | `server/src/utils/logger.ts` + `llmGateway.ts` + `agents.ts` | 全链路彩色结构化日志 |

### 批次 2: Phase 1-3 编排升级

| 修复 | 文件 | 内容 |
|------|------|------|
| DAG 死锁 | `scheduler.ts` | `dependsOn` 检查 `failed` 集合，上游失败不阻塞下游 |
| 产物绑定 | `scheduler.ts` | `artifact-draft` chunk 时填充 `task.producedArtifactId` |
| paused 状态 | `types.ts` | 新增 `'paused'` SubTaskStatus |
| 上游上下文注入 | `scheduler.ts` + `remoteAgent.ts` | `buildUpstreamContext()` → `AgentInput.upstreamContext` → system prompt |
| GenUI 检测 | `fenceExtractor.ts` | ` ```ui { "component": "ChoiceCards" } ``` ` → `ui-component` chunk |
| 暂停/恢复 | `scheduler.ts` + `appStore.ts` | `onUiPause`→ 轮询用户回复 → `resumePausedTask` → 下游自动触发 |
| 超时保护 | `appStore.ts` | 30 分钟无响应自动用默认选项继续 |

### 批次 3: Phase 4-5 产物合并 + 质量保证 + 韧性

| 修复 | 文件 | 内容 |
|------|------|------|
| 产物合并 | `pmoSynthesizer.ts` | `merge()` 引用完整性检测 + `mergeArtifacts()` 自包含输出 |
| Critic 集成 | `appStore.ts` | 每个完成任务 → `reviewTask()` → 评分写入 plan card → PMO 报告 |
| 进度面板 | `PlanCard.tsx` + `BlackboardCard.tsx` + `MessageBubble.tsx` | 实时字符计数、暂停图标、阶段指示器、Blackboard 内联卡片 |
| Agent 降级 | `agentSelector.ts` + `appStore.ts` | 全局失败计数 → ≥3 次施加 50% 评分惩罚 |
| 超时恢复 | `scheduler.ts` | 计划超时保留已完成产物，不丢弃部分输出 |
| 连接恢复 | `scheduler.ts` | `resumePausedTask` 增加指数退避重试 |

### 批次 4: Context-Bug-Fix-V1 — 消息持久化

| 修复 | 文件 | 内容 |
|------|------|------|
| `addMsg` 自动持久化 | `appStore.ts` | 所有消息路径自动调用 `apiPostMessage`，流式消息延迟到 `streaming=false` 时持久化 |
| `patchMsg` 最终持久化 | `appStore.ts` | 检测 `streaming: true→false` 转换时触发持久化 |
| 去重手动持久化 | `appStore.ts` | 移除 `sendUserMessage` / `runSingleAgent` 中重复的 `apiPostMessage` |
| SelectionContext | `types.ts` | 新增接口：artifactId, versionId, selectedText, offsets |
| 产物上下文传递 | `ArtifactPanel.tsx` + `appStore.ts` + `remoteAgent.ts` | `contextArtifacts` 完整文件注入 Agent system prompt |

### 批次 5: LongTask-Bug-Fix-V1 — 长任务 7 根因修复

| Fix | 文件 | 根因 | 效果 |
|-----|------|------|------|
| A | `ArtifactPanel.tsx` | `mentions=[]` 硬编码 | 选谁就调谁，不走 PMO |
| C | `appStore.ts` + `scheduler.ts` | 编排路径丢上下文 | PMO 编排也拿完整文件 |
| D | `appStore.ts` | 跨 Agent 同名 artifact | 同轮去重：5min内不同Agent加后缀区分 |
| E | `fenceExtractor.ts` | feed() 无空保护 | 空白 artifact 不再出现 |
| F | `remoteAgent.ts` | catch 不 flush | 网络异常时代码部分保留 |
| G | `llmGateway.ts` | max_tokens=4096 | → 8192（5 处） |
| — | `server/src/index.ts` | express.json 默认 100KB | → 10MB |

---

## 三、改动的文件汇总

| 文件 | 批次 | 改动类型 |
|------|------|---------|
| `server/src/routes/agents.ts` | 1 | 新增 agent_doc + 请求日志 |
| `server/src/services/llmGateway.ts` | 1, 5 | 模型切换 + max_tokens + 全链路日志 |
| `server/src/utils/logger.ts` | 1 | 新建：结构化彩色日志工具 |
| `server/src/index.ts` | 5 | express.json limit 100KB→10MB |
| `agenthub-mvp/index.html` | 1 | 移除 CSP meta 标签 |
| `agenthub-mvp/src/types.ts` | 2, 4 | paused 状态 + upstreamContext + ui-component + SelectionContext + OrchestratorSession + blackboard MessageContent |
| `agenthub-mvp/src/api/client.ts` | 2 | AgentChunk 类型新增 ui-component |
| `agenthub-mvp/src/agents/fenceExtractor.ts` | 2, 5 | GenUI 检测 + 空围栏保护 |
| `agenthub-mvp/src/agents/remoteAgent.ts` | 2, 4, 5 | 上游上下文注入 + contextArtifacts + catch flush |
| `agenthub-mvp/src/orchestrator/scheduler.ts` | 2, 3, 5 | DAG 修复 + 产物绑定 + 上游上下文 + 暂停/恢复 + 超时恢复 + contextArtifacts 线程 |
| `agenthub-mvp/src/orchestrator/index.ts` | 2, 3 | 导出 inferCapabilities + resumePausedTask + getBlackboard |
| `agenthub-mvp/src/orchestrator/supervisor.ts` | 2 | 导出 inferCapabilities |
| `agenthub-mvp/src/orchestrator/pmoSynthesizer.ts` | 3 | merge() + mergeArtifacts() + MergeReport |
| `agenthub-mvp/src/orchestrator/agentSelector.ts` | 3 | failurePenaltyMap 降级评分 |
| `agenthub-mvp/src/orchestrator/blackboard.ts` | 3 | 导出 getBlackboard |
| `agenthub-mvp/src/store/appStore.ts` | 1-5 | 编排器接入 + GenUI 暂停/恢复 + 持久化 + 产物上下文 + 同轮去重 + Critic 集成 |
| `agenthub-mvp/src/components/ArtifactPanel.tsx` | 1, 4, 5 | Monaco key + SelectionRail mentions + 上下文传递 |
| `agenthub-mvp/src/components/PlanCard.tsx` | 3 | 实时计数 + 暂停图标 + 阶段指示器 |
| `agenthub-mvp/src/components/BlackboardCard.tsx` | 3 | 飞书风格 compact 视图 |
| `agenthub-mvp/src/components/MessageBubble.tsx` | 3 | BlackboardCard + PlanCard convId |
| `agenthub-mvp/src/components/icons.tsx` | 3 | PauseCircle 图标 |

---

## 四、测试覆盖

- 前端: 13 个测试文件, 163 个测试 → ✅ 全部通过
- 后端: 2 个测试文件, 28 个测试 → ✅ 全部通过
- TypeScript: 前端 + 后端 → ✅ 零错误
